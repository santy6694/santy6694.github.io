source "https://rubygems.org"

gem "jekyll", "~> 4.3"

# Needed for `jekyll serve` on Ruby 3+ (webrick isn't bundled with Ruby anymore)
gem "webrick", "~> 1.8"

group :jekyll_plugins do
  # add Jekyll plugins here if needed
end
